package fr.marc.paymybuddy.service;

import static org.assertj.core.api.Assertions.assertThat;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

import fr.marc.paymybuddy.model.User;


public class UserServiceTest {
	
	/*
	@Autowired
	private UserService userService;
	
	static Logger log = LogManager.getLogger(UserServiceTest.class.getName());
	
	private User user1 = new User(1,"acall@mail.fr","Arthur","Call","Excalibur",false,"FR00123456789","Camelot");
	
	@Test
	public void getUsers_success() {

		final Iterable<User> resultList = userService.getUsers();
		
		log.error("List of users"+resultList);

		assertThat(resultList).contains(user1);
	}
	*/

}
